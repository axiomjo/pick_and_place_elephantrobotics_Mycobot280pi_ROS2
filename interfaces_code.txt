==========
CMakeLists.txt
======

cmake_minimum_required(VERSION 3.8)
project(mycobot280pi_interfaces)

# This CMake file tells ROS2 how to build and install your custom interface files (messages, services, actions).
# Non-technical readers: You usually don't need to change this unless you add new interface files or dependencies.

if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
  add_compile_options(-Wall -Wextra -Wpedantic)
endif()

# find dependencies
find_package(ament_cmake REQUIRED)
find_package(std_msgs REQUIRED)
find_package(geometry_msgs REQUIRED)
find_package(sensor_msgs REQUIRED)

# Find the ROS2 interface generation tools
find_package(rosidl_default_generators REQUIRED)

if(BUILD_TESTING)
  find_package(ament_lint_auto REQUIRED)
  # the following line skips the linter which checks for copyrights
  # uncomment the line when a copyright and license is not present in all source files
  #set(ament_cmake_copyright_FOUND TRUE)
  # the following line skips cpplint (only works in a git repo)
  # uncomment the line when this package is not in a git repo
  #set(ament_cmake_cpplint_FOUND TRUE)
  ament_lint_auto_find_test_dependencies()
endif()


   
  # List your custom message, service, and action files here
  set(msg_files
    "msg/OneDetectedObject.msg"
    "msg/ManyDetectedObjects.msg"
    "msg/Point2D.msg"
    "msg/Point2DArray.msg"
    "msg/SimpleCommands.msg"
    "msg/JointAnglesArray.msg"
  )

  set(srv_files
    "srv/Mycobot280PiSimpleCommandsMadeSure.srv"
  )

  set(action_files
    "action/ProcessWorkspace.action"
    "action/SimpleCommandsAction.action"
  )

  # Generate code for the listed interfaces
  rosidl_generate_interfaces(${PROJECT_NAME}
    ${msg_files}
    ${srv_files}
    ${action_files}
    DEPENDENCIES std_msgs geometry_msgs sensor_msgs
  )

ament_package()


=====

==========
package.xml
======

<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>mycobot280pi_interfaces</name>
  <version>0.0.0</version>
  <description>TODO: Package description</description>
  <maintainer email="jojo.josephined@gmail.com">axiomjo</maintainer>
  <license>TODO: License declaration</license>

  <!--
    The following dependencies and instructions are required for ROS2 to build
    custom message (.msg), service (.srv), and action (.action) interface files.
    These allow different parts of the robot system to communicate using
    structured data types.
  -->
  
  <member_of_group>rosidl_interface_packages</member_of_group>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <!-- Core dependencies for building and using custom interfaces -->
  <build_depend>rosidl_default_generators</build_depend>
  <exec_depend>rosidl_default_runtime</exec_depend>

  <!-- Standard message types used in our custom interfaces (e.g., Header) -->
  <build_depend>std_msgs</build_depend>
  <exec_depend>std_msgs</exec_depend>
  <build_depend>geometry_msgs</build_depend>
  <exec_depend>geometry_msgs</exec_depend>
  <build_depend>sensor_msgs</build_depend>
  <exec_depend>sensor_msgs</exec_depend>

  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
    <!--
      This tag ensures that the generated interfaces are available
      to other ROS2 packages that depend on this one.
    -->
    <rosidl_generate_interfaces/>
  </export>
</package>


=====

==========
srv/Mycobot280PiSimpleCommandsMadeSure.srv
======

# Interface type: mycobot280pi_interfaces/srv/Mycobot280PiSimpleCommandsMadeSure
# Filepath: src/mycobot280pi_interfaces/srv/Mycobot280PiSimpleCommandsMadeSure.srv
# Mycobot280PiSimpleCommandsMadeSure.srv

# Request
string command_type         # e.g. "move", "vacuum_on", "vacuum_off", "vacuum_weak", "set_rgb", "set_joint_angles", etc.

# For movement commands
float32[] coords           # [x, y, z, rx, ry, rz] for move commands (optional)
float32[] joint_angles     # For direct joint control (optional)
int32 speed                # Movement speed (optional)

# For RGB commands
int32 r                    # Red value (0-255)
int32 g                    # Green value (0-255)
int32 b                    # Blue value (0-255)

# for vacuum pump V2
uint8 vacuum_pin1_level   # (white wire) 
uint8 vacuum_pin2_level   # (yellow wire) HIGH/LOW

# For future extensibility, you can add:
string[] extra_strings     # For any extra string parameters
float32[] extra_floats     # For any extra float parameters
int32[] extra_ints         # For any extra int parameters
---
# Response
bool success
string message


=====

==========
action/ProcessWorkspace.action
======

# Interface type: mycobot280pi_interfaces/action/ProcessWorkspace
# Filepath: src/mycobot280pi_interfaces/action/ProcessWorkspace.action
# GOAL
# all should match in order
ManyDetectedObjects objects_to_move
Point2DArray objects_target_position
int32[] objects_target_orientation
---
# Result
bool success
string message

---
# Feedback
string current_state
OneDetectedObject current_object


=====

==========
action/SimpleCommandsAction.action
======

# Interface type: mycobot280pi_interfaces/action/SimpleCommandsAction
# Filepath: src/mycobot280pi_interfaces/action/SimpleCommandsAction.action
# GOAL
string command_type         # e.g. "move", "vacuum_on", "vacuum_off", "vacuum_weak", "set_rgb", "set_joint_angles", etc.

# For movement commands
float32[] coords           # [x, y, z, rx, ry, rz] for move commands (optional)
float32[] joint_angles     # For direct joint control (optional)
int32 speed                # Movement speed (optional)

# For RGB commands
int32 r                    # Red value (0-255)
int32 g                    # Green value (0-255)
int32 b                    # Blue value (0-255)

---
# Result
bool success
string message

---
# Feedback
string current_state



=====

==========
msg/JointAnglesArray.msg
======

# Interface type: mycobot280pi_interfaces/msg/JointAnglesArray
# Filepath: src/mycobot280pi_interfaces/msg/JointAnglesArray.msg
# Used in: /robot/msg_joint_angles
float64[] joint_angles


=====

==========
msg/ManyDetectedObjects.msg
======

# Interface type: mycobot280pi_interfaces/msg/ManyDetectedObjects
# Filepath: src/mycobot280pi_interfaces/msg/ManyDetectedObjects.msg
# Uses: OneDetectedObject (as objects[])
# Used in: /vision/detected_objects topic, planner_robot_node, gui_robot_control_node
#ManyDetectedObjects.msg

std_msgs/Header header
OneDetectedObject[] objects


=====

==========
msg/OneDetectedObject.msg
======

# Interface type: mycobot280pi_interfaces/msg/OneDetectedObject
# Filepath: src/mycobot280pi_interfaces/msg/OneDetectedObject.msg
# Uses: Point2D (as center_point)
# Used in: ManyDetectedObjects (as objects[]), ProcessWorkspace.action (as objects_to_move[])
#OneDetectedObject.msg

int32 id                 # assigned number upon detection

Point2D center_point     # bounding box center point
int32 width              # bounding box width
int32 height             # bounding box height



=====

==========
msg/Point2D.msg
======

# Interface type: mycobot280pi_interfaces/msg/Point2D
# Filepath: src/mycobot280pi_interfaces/msg/Point2D.msg
# Used in: Point2DArray (as an array of points), OneDetectedObject (as center_point)
float32 x
float32 y



=====

==========
msg/Point2DArray.msg
======

# Interface type: mycobot280pi_interfaces/msg/Point2DArray
# Filepath: src/mycobot280pi_interfaces/msg/Point2DArray.msg
# Uses: Point2D (as points[])
# Used in: /vision/perspective_points topic, ProcessWorkspace.action (as objects_target_position)
Point2D[] points



=====

==========
msg/SimpleCommands.msg
======

# Interface type: mycobot280pi_interfaces/msg/SimpleCommands
# Filepath: src/mycobot280pi_interfaces/msg/SimpleCommands.msg
# SimpleCommands.msg
# A flexible message for atomic robot commands from planner to executor

string command_type         # e.g. "move", "vacuum_on", "vacuum_off", "vacuum_weak", "set_rgb", "set_joint_angles", etc.

# For movement commands
float32[] coords           # [x, y, z, rx, ry, rz] for move commands (optional)
float32[] joint_angles     # For direct joint control (optional)
int32 speed                # Movement speed (optional)

# For RGB commands
int32 r                    # Red value (0-255)
int32 g                    # Green value (0-255)
int32 b                    # Blue value (0-255)

# for vacuum pump V2
uint8 vacuum_pin1_level   # (white wire) 
uint8 vacuum_pin2_level   # (yellow wire) HIGH/LOW

# For future extensibility, you can add:
string[] extra_strings     # For any extra string parameters
float32[] extra_floats     # For any extra float parameters
int32[] extra_ints         # For any extra int parameters


=====

